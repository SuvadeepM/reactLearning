import InputBox from "./InputBox";

// In bigger projects, it's a common practice to export all the components from a single file so that we don't have to necessarily import components all the time. By doing this, we can import only index.js and that's it.

export { InputBox };
